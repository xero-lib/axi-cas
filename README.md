# AxiCAS
[Axi](https://github.com/xero-lib/axi)'s CAS runtime.

Performant, versatile, and (eventually) embedded-safe CAS library, written in Rust because it's fun.
Currently uses crates heavily, but will eventually be all in-house for maximum optimization and minimal footprint.

For a full frontend, see Axi, linked above.
